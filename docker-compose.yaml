version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sora-henkan-postgres
    environment:
      POSTGRES_USER: sorahenkan
      POSTGRES_PASSWORD: sorahenkan_dev_password
      POSTGRES_DB: sorahenkan
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sorahenkan -d sorahenkan"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sora-henkan-network

  # MinIO for object storage (optional - uncomment if needed)
  # minio:
  #   image: minio/minio:latest
  #   container_name: sora-henkan-minio
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   volumes:
  #     - minio_data:/data
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - sora-henkan-network

volumes:
  postgres_data:
    driver: local
  # minio_data:
  #   driver: local

networks:
  sora-henkan-network:
    driver: bridge
